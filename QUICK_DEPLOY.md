# 🚀 Быстрая установка обновлений на сервер

## Вариант 1: Автоматическая установка (рекомендуется)

```bash
# 1. Подключитесь к серверу
ssh user@your-server.com
cd /path/to/pdftest

# 2. Сделайте скрипт исполняемым и запустите
chmod +x deploy.sh
./deploy.sh
```

## Вариант 2: Ручная установка

### 1. Обновите файлы на сервере
```bash
# Через Git
git pull

# Или скопируйте файлы вручную:
# - models.py
# - app.py
# - templates/admin_subjects.html
# - static/js/editor.js
# - alembic/versions/2025_01_27_000001_add_subjects_and_class_fields.py
```

### 2. Остановите приложение
```bash
sudo systemctl stop pdftest
# или
sudo supervisorctl stop pdftest
```

### 3. Установите зависимости
```bash
source venv/bin/activate
pip install transliterate
```

### 4. Примените миграцию БД

**Через Alembic:**
```bash
alembic upgrade head
```

**Или вручную (SQL):**
```bash
# Для PostgreSQL
psql -h 185.22.64.9 -U flask_user -d flask_db < migration_manual.sql

# Для SQLite
sqlite3 local_dev.db < migration_manual.sql
```

### 5. Запустите приложение
```bash
sudo systemctl start pdftest
# или
sudo supervisorctl start pdftest
```

### 6. Создайте предметы
1. Войдите как супер-пользователь (`baseke`)
2. Перейдите в "📚 Управление предметами"
3. Создайте предметы (Математика, Русский язык и т.д.)

## Проверка

После установки проверьте:
- ✅ `/admin/subjects` доступна
- ✅ Можно создать предмет
- ✅ В редакторе есть выбор класса и предмета
- ✅ Ссылки генерируются в формате: `/test/<city>/<school>/<subject>/<topic>`

---

**Подробная инструкция:** см. `DEPLOYMENT_GUIDE.md`

