# 📋 Пошаговая инструкция по установке обновлений на сервер

## Дата обновления: 2025-01-27

### Что было добавлено:
- ✅ Модель Subject (предметы)
- ✅ Поля class_number и subject_id в Template
- ✅ API для управления предметами
- ✅ Новый формат ссылок: `/test/<city_code>/<school_code>/<subject_slug>/<topic_slug>`
- ✅ Интерфейс выбора класса и предмета в редакторе
- ✅ Страница управления предметами для супер-пользователя

---

## 🔧 Шаг 1: Резервное копирование

**ВАЖНО!** Перед обновлением создайте резервную копию базы данных и файлов.

### 1.1. Резервная копия PostgreSQL (если используется):
```bash
# Подключитесь к серверу
ssh user@your-server.com

# Создайте резервную копию БД
pg_dump -h 185.22.64.9 -U flask_user -d flask_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Или через psql
pg_dump -h 185.22.64.9 -U flask_user flask_db > backup_before_update.sql
```

### 1.2. Резервная копия SQLite (если используется локально):
```bash
# Скопируйте файл БД
cp local_dev.db backup_local_dev_$(date +%Y%m%d_%H%M%S).db
```

### 1.3. Резервная копия файлов проекта:
```bash
# Создайте архив текущей версии
tar -czf backup_project_$(date +%Y%m%d_%H%M%S).tar.gz /path/to/project/
```

---

## 📦 Шаг 2: Обновление файлов на сервере

### 2.1. Подключение к серверу:
```bash
ssh user@your-server.com
cd /path/to/pdftest  # Перейдите в директорию проекта
```

### 2.2. Остановка приложения (если запущено):
```bash
# Если используете systemd
sudo systemctl stop pdftest

# Или если используете supervisor
sudo supervisorctl stop pdftest

# Или если запущено через screen/tmux
# Найдите процесс и остановите его
ps aux | grep python
kill <PID>
```

### 2.3. Обновление кода (через Git или вручную):

**Вариант A: Через Git (рекомендуется)**
```bash
# Сохраните текущие изменения (если есть)
git stash

# Получите последние изменения
git pull origin main  # или master, в зависимости от вашей ветки

# Примените сохраненные изменения (если были)
git stash pop
```

**Вариант B: Вручную (если нет Git)**
```bash
# Скопируйте обновленные файлы на сервер через scp или rsync
# С вашего локального компьютера:
scp models.py user@server:/path/to/pdftest/
scp app.py user@server:/path/to/pdftest/
scp -r templates/ user@server:/path/to/pdftest/
scp -r static/ user@server:/path/to/pdftest/
scp -r alembic/versions/ user@server:/path/to/pdftest/alembic/
```

### 2.4. Проверка обновленных файлов:
```bash
# Убедитесь, что все файлы на месте
ls -la models.py app.py
ls -la templates/admin_subjects.html
ls -la alembic/versions/2025_01_27_000001_add_subjects_and_class_fields.py
```

---

## 🗄️ Шаг 3: Обновление базы данных

### 3.1. Активация виртуального окружения:
```bash
# Если используете venv
source venv/bin/activate

# Или если используете conda
conda activate pdftest
```

### 3.2. Установка зависимостей (если нужно):
```bash
# Проверьте, установлен ли transliterate (нужен для генерации slug)
pip install transliterate

# Обновите зависимости
pip install -r requirements.txt
```

### 3.3. Применение миграций:

**Вариант A: Через Alembic (рекомендуется)**
```bash
# Проверьте текущую версию БД
alembic current

# Примените миграции
alembic upgrade head

# Проверьте, что миграция применена
alembic current
```

**Вариант B: Вручную (если Alembic не работает)**

Если Alembic не установлен или не работает, выполните SQL вручную:

**Для PostgreSQL:**
```bash
psql -h 185.22.64.9 -U flask_user -d flask_db
```

Затем выполните SQL из файла миграции:
```sql
-- Создание таблицы subjects
CREATE TABLE IF NOT EXISTS subjects (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200) NOT NULL UNIQUE,
    name_slug VARCHAR(200) NOT NULL UNIQUE,
    description TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Создание индексов для subjects
CREATE INDEX IF NOT EXISTS ix_subjects_name_slug ON subjects(name_slug);
CREATE INDEX IF NOT EXISTS ix_subjects_is_active ON subjects(is_active);

-- Добавление полей в templates
ALTER TABLE templates ADD COLUMN IF NOT EXISTS class_number INTEGER;
ALTER TABLE templates ADD COLUMN IF NOT EXISTS subject_id INTEGER;

-- Создание индекса на subject_id
CREATE INDEX IF NOT EXISTS ix_templates_subject_id ON templates(subject_id);

-- Удаление старого индекса (если существует)
DROP INDEX IF EXISTS idx_username_topic_slug;

-- Создание нового уникального индекса
CREATE UNIQUE INDEX IF NOT EXISTS idx_username_subject_topic 
ON templates(created_by_username, subject_id, topic_slug);
```

**Для SQLite:**
```bash
sqlite3 local_dev.db
```

Затем выполните SQL:
```sql
-- Создание таблицы subjects
CREATE TABLE IF NOT EXISTS subjects (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(200) NOT NULL UNIQUE,
    name_slug VARCHAR(200) NOT NULL UNIQUE,
    description TEXT,
    is_active BOOLEAN DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Создание индексов для subjects
CREATE INDEX IF NOT EXISTS ix_subjects_name_slug ON subjects(name_slug);
CREATE INDEX IF NOT EXISTS ix_subjects_is_active ON subjects(is_active);

-- Добавление полей в templates
ALTER TABLE templates ADD COLUMN class_number INTEGER;
ALTER TABLE templates ADD COLUMN subject_id INTEGER;

-- Создание индекса на subject_id
CREATE INDEX IF NOT EXISTS ix_templates_subject_id ON templates(subject_id);

-- Удаление старого индекса (если существует)
DROP INDEX IF EXISTS idx_username_topic_slug;

-- Создание нового уникального индекса
CREATE UNIQUE INDEX IF NOT EXISTS idx_username_subject_topic 
ON templates(created_by_username, subject_id, topic_slug);
```

### 3.4. Проверка миграций:
```bash
# Проверьте структуру таблиц
# Для PostgreSQL:
psql -h 185.22.64.9 -U flask_user -d flask_db -c "\d subjects"
psql -h 185.22.64.9 -U flask_user -d flask_db -c "\d templates"

# Для SQLite:
sqlite3 local_dev.db ".schema subjects"
sqlite3 local_dev.db ".schema templates"
```

---

## 🧪 Шаг 4: Проверка и тестирование

### 4.1. Проверка конфигурации:
```bash
# Убедитесь, что .env файл настроен правильно
cat .env | grep DATABASE_URL
cat .env | grep SUPERUSER_PASSWORD
```

### 4.2. Тестовый запуск приложения:
```bash
# Запустите приложение в режиме отладки
python app.py

# Или через Flask
export FLASK_APP=app.py
flask run --host=0.0.0.0 --port=5000
```

### 4.3. Проверка доступности:
```bash
# В другом терминале проверьте, что приложение отвечает
curl http://localhost:5000/health

# Проверьте API предметов
curl http://localhost:5000/api/subjects
```

### 4.4. Проверка через браузер:
1. Откройте `http://your-server:5000` (или ваш домен)
2. Войдите как супер-пользователь (`baseke`)
3. Перейдите в "📚 Управление предметами"
4. Создайте тестовый предмет (например, "Математика")
5. Войдите как учитель
6. Создайте тест с выбором класса и предмета
7. Проверьте, что ссылка генерируется в новом формате

---

## 🚀 Шаг 5: Запуск в production

### 5.1. Остановите тестовый запуск:
```bash
# Нажмите Ctrl+C в терминале, где запущено приложение
```

### 5.2. Запуск через systemd:
```bash
sudo systemctl start pdftest
sudo systemctl status pdftest
```

### 5.3. Запуск через supervisor:
```bash
sudo supervisorctl start pdftest
sudo supervisorctl status pdftest
```

### 5.4. Запуск через screen/tmux (если используется):
```bash
screen -S pdftest
# или
tmux new -s pdftest

# Запустите приложение
python app.py
# или
gunicorn -w 4 -b 0.0.0.0:5000 app:app

# Отключитесь: Ctrl+A, затем D (для screen) или Ctrl+B, затем D (для tmux)
```

---

## ✅ Шаг 6: Создание начальных данных

### 6.1. Войдите как супер-пользователь:
1. Откройте `https://docquiz.predmet.kz` (или ваш домен)
2. Войдите с логином `baseke` и паролем (из переменной `SUPERUSER_PASSWORD` или `changeme123`)

### 6.2. Создайте предметы:
1. Перейдите в "📚 Управление предметами" (или `/admin/subjects`)
2. Создайте предметы, например:
   - Математика
   - Русский язык
   - Физика
   - Химия
   - Биология
   - и т.д.

### 6.3. Проверьте работу:
1. Войдите как учитель
2. Создайте тест:
   - Выберите класс (1-11)
   - Выберите предмет
   - Введите тему теста
   - Сохраните
3. Проверьте, что ссылка имеет формат:
   `https://docquiz.predmet.kz/test/<city_code>/<school_code>/<subject_slug>/<topic_slug>`

---

## 🔍 Шаг 7: Проверка логов

### 7.1. Проверка логов приложения:
```bash
# Если используете systemd
sudo journalctl -u pdftest -f

# Если логи в файле
tail -f /var/log/pdftest/app.log

# Или если логи в stdout
# Проверьте вывод в screen/tmux
```

### 7.2. Проверка ошибок:
```bash
# Проверьте логи на наличие ошибок
grep -i error /var/log/pdftest/app.log

# Проверьте доступность БД
python -c "from models import SessionLocal; db = SessionLocal(); print('DB OK'); db.close()"
```

---

## 🆘 Шаг 8: Откат изменений (если что-то пошло не так)

### 8.1. Откат миграций:
```bash
# Если используете Alembic
alembic downgrade -1

# Или вручную удалите изменения из БД
# (используйте SQL из функции downgrade() в миграции)
```

### 8.2. Восстановление файлов:
```bash
# Восстановите файлы из резервной копии
tar -xzf backup_project_YYYYMMDD_HHMMSS.tar.gz -C /path/to/project/
```

### 8.3. Восстановление БД:
```bash
# Для PostgreSQL
psql -h 185.22.64.9 -U flask_user -d flask_db < backup_before_update.sql

# Для SQLite
cp backup_local_dev_YYYYMMDD_HHMMSS.db local_dev.db
```

---

## 📝 Чек-лист после установки

- [ ] Резервная копия создана
- [ ] Файлы обновлены на сервере
- [ ] Миграции применены успешно
- [ ] Таблица `subjects` создана
- [ ] Поля `class_number` и `subject_id` добавлены в `templates`
- [ ] Индексы созданы
- [ ] Приложение запускается без ошибок
- [ ] Страница управления предметами доступна
- [ ] Можно создать предмет
- [ ] Можно создать тест с классом и предметом
- [ ] Ссылка генерируется в новом формате
- [ ] Старые ссылки работают (обратная совместимость)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи приложения
2. Проверьте логи БД
3. Убедитесь, что все зависимости установлены
4. Проверьте права доступа к файлам и БД

---

**Удачи с обновлением! 🚀**


