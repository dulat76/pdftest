# 📝 PDF Testing System with AI-Powered Grading

Веб-приложение для автоматизированного тестирования студентов с интеллектуальной проверкой ответов на основе AI.

## ✨ Возможности

- 📄 **Загрузка PDF** - автоматическая конвертация в изображения
- 🎯 **Создание тестов** - визуальный редактор с привязкой полей к изображениям
- 🤖 **Умная проверка ответов**:
  - Точное совпадение и нормализация
  - Fuzzy matching (прощает опечатки)
  - Семантический анализ (понимает синонимы)
  - AI-проверка через Google Gemini
- 📊 **Интеграция с Google Sheets** - автоматическое сохранение результатов
- 💾 **Кэширование** - PostgreSQL кэш для AI ответов
- 🔐 **Аутентификация** - защита редактора паролем

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11+
- PostgreSQL 15+ (опционально, для кэширования)
- Redis (опционально, для продвинутого кэширования)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd pdftest
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения**
```bash
cp .env.example .env
# Отредактируйте .env и добавьте свои ключи
```

5. **Получите Google API ключ**
   - Перейдите на https://makersuite.google.com/app/apikey
   - Создайте новый API ключ
   - Добавьте в `.env`: `GOOGLE_API_KEY=your_key_here`

6. **Настройте Google Sheets (опционально)**
   - Создайте Service Account в Google Cloud Console
   - Скачайте `credentials.json`
   - Поместите в папку `credentials/`

7. **Инициализируйте базу данных (опционально)**
```bash
python init_cache_db.py
```

8. **Загрузите языковую модель spaCy**
```bash
python -m spacy download ru_core_news_sm
```

### Запуск

**Режим разработки:**
```bash
python app.py
```

**Продакшн (Gunicorn):**
```bash
gunicorn -c gunicorn_conf.py app:app
```

**Docker:**
```bash
docker-compose up -d
```

Приложение будет доступно по адресу: http://localhost:5000

## 📁 Структура проекта

```
pdftest/
├── app.py                  # Главное приложение Flask
├── config.py               # Конфигурация приложения
├── ai_config.py            # Настройки AI
├── ai_checker.py           # AI проверка ответов (Gemini)
├── answer_scorer.py        # Локальный скорер (fuzzy + semantic)
├── ai_cache.py             # Кэширование AI ответов
├── auth_utils.py           # Аутентификация
├── requirements.txt        # Python зависимости
├── requirements-dev.txt    # Зависимости для разработки
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Docker Compose конфигурация
├── .env.example            # Шаблон переменных окружения
├── SECURITY.md             # Документация по безопасности
├── templates/              # HTML шаблоны
├── static/                 # CSS, JS, изображения
├── templates_json/         # JSON шаблоны тестов
├── uploads/                # Загруженные файлы
├── credentials/            # Google API credentials
├── logs/                   # Логи приложения
└── tests/                  # Тесты
```

## 🔧 Конфигурация

### Основные настройки (config.py)

- `DEBUG` - режим отладки (False в продакшене!)
- `SECRET_KEY` - секретный ключ Flask
- `PDF_DPI` - разрешение для конвертации PDF (по умолчанию 200)
- `SESSION_TIMEOUT_HOURS` - таймаут сессии (по умолчанию 2 часа)

### AI настройки (ai_config.py или веб-интерфейс /ai-settings)

- `AI_CHECKING_ENABLED` - включить/выключить AI проверку
- `LOCAL_SCORER_ENABLED` - использовать локальный скорер
- `FUZZY_STRICT` - порог для строгого fuzzy matching (95)
- `FUZZY_SOFT` - порог для мягкого fuzzy matching (90)
- `SEM_THRESHOLD` - порог семантической схожести (0.75)
- `GEMINI_MODEL` - модель Gemini (по умолчанию gemini-2.0-flash)

## 🎓 Использование

### Для преподавателей

1. Войдите в систему: `/login`
2. Откройте редактор: `/editor`
3. Загрузите PDF или изображения
4. Создайте поля для ответов
5. Укажите правильные варианты ответов
6. Сохраните шаблон
7. Поделитесь ссылкой со студентами

### Для студентов

1. Откройте страницу студента: `/student`
2. Выберите тест из списка
3. Заполните поля ответов
4. Нажмите "Проверить"
5. Получите результаты с объяснениями

## 🧪 Тестирование

```bash
# Установите dev зависимости
pip install -r requirements-dev.txt

# Запустите тесты
pytest

# С покрытием
pytest --cov=. --cov-report=html

# Линтинг
black .
flake8 .
mypy .
```

## 🐳 Docker

```bash
# Сборка образа
docker build -t pdftest .

# Запуск с docker-compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 📊 API Endpoints

### Публичные
- `GET /` - Главная страница
- `GET /student` - Страница студента
- `GET /list_templates` - Список доступных тестов
- `POST /check_answers` - Проверка ответов

### Защищённые (требуют аутентификации)
- `GET /editor` - Редактор тестов
- `POST /upload` - Загрузка файлов
- `POST /save_template` - Сохранение шаблона
- `GET /ai-settings` - Настройки AI
- `GET /api/ai/status` - Статус AI системы
- `GET /api/ai/stats` - Статистика проверок
- `POST /api/ai/test` - Тестирование AI

## 🔐 Безопасность

⚠️ **ВАЖНО:** Прочитайте [SECURITY.md](SECURITY.md) перед развертыванием!

- Никогда не коммитьте `.env` файл
- Используйте сильные пароли и SECRET_KEY
- Включите HTTPS в продакшене
- Регулярно обновляйте зависимости
- Ротируйте API ключи при компрометации

## 🛠️ Разработка

### Установка pre-commit hooks

```bash
pip install pre-commit
pre-commit install
```

### Добавление новых зависимостей

```bash
pip install package-name
pip freeze > requirements.txt
```

### Миграции базы данных

```bash
# Создать миграцию
alembic revision --autogenerate -m "Description"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1
```

## 📈 Мониторинг

- Логи: `logs/ai_checks.log`
- Health check: `/health`
- Метрики: `/metrics` (если настроен Prometheus)

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

[Укажите вашу лицензию]

## 👥 Авторы

[Укажите авторов]

## 🙏 Благодарности

- Google Gemini API
- spaCy для NLP
- sentence-transformers для семантического анализа
- Flask и экосистема Python

## 📞 Поддержка

- Issues: [GitHub Issues](your-repo-url/issues)
- Email: your-email@example.com

## 🗺️ Roadmap

- [ ] Поддержка нескольких языков (i18n)
- [ ] Расширенная аналитика для преподавателей
- [ ] Мобильное приложение
- [ ] Интеграция с LMS системами
- [ ] Поддержка математических формул
- [ ] OCR для рукописных ответов
- [ ] Gamification (рейтинги, достижения)
- [ ] PWA для offline режима

---

Made with ❤️ for education
