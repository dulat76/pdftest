# 📝 Инструкция по сохранению проекта в Git

## Быстрая команда (все изменения):

```bash
# 1. Добавить все изменения
git add .

# 2. Создать коммит с описанием
git commit -m "Добавлена система управления предметами и классами

- Добавлена модель Subject для предметов
- Добавлены поля class_number и subject_id в Template
- Реализован API для управления предметами (CRUD)
- Обновлен формат ссылок: /test/<city>/<school>/<subject>/<topic>
- Добавлен выбор класса (1-11) и предмета в редакторе
- Создана страница управления предметами для супер-пользователя
- Добавлены миграции БД
- Обновлена документация по развертыванию"

# 3. Отправить на сервер
git push origin master
```

## Пошаговая инструкция:

### Шаг 1: Проверка статуса
```bash
git status
```

### Шаг 2: Добавление файлов

**Вариант A: Добавить все файлы**
```bash
git add .
```

**Вариант B: Добавить конкретные файлы**
```bash
# Основные файлы
git add app.py models.py auth_utils.py validators.py utils.py

# Шаблоны
git add templates/editor.html templates/admin_subjects.html templates/admin_teachers.html

# JavaScript
git add static/js/editor.js

# Миграции
git add alembic/versions/2025_01_27_*.py

# Документация
git add DEPLOYMENT_GUIDE.md QUICK_DEPLOY.md migration_manual.sql

# Скрипты
git add deploy.sh init_superuser.py
```

### Шаг 3: Создание коммита
```bash
git commit -m "Краткое описание изменений"
```

**Примеры сообщений коммита:**
```bash
# Короткое
git commit -m "Добавлена система управления предметами"

# Подробное (многострочное)
git commit -m "Добавлена система управления предметами и классами

- Добавлена модель Subject
- Реализован API для предметов
- Обновлен формат ссылок на тесты
- Добавлен выбор класса и предмета в редакторе"
```

### Шаг 4: Отправка на сервер
```bash
# Отправить в основную ветку
git push origin master

# Или если ваша ветка называется main
git push origin main
```

## Проверка перед коммитом:

### 1. Проверьте, что не добавляете лишние файлы:
```bash
git status
```

### 2. Посмотрите, что будет добавлено:
```bash
git diff --cached
```

### 3. Проверьте .gitignore:
Убедитесь, что в `.gitignore` есть:
- `.env` (секреты)
- `*.db` (базы данных)
- `venv/` (виртуальное окружение)
- `uploads/*` (загруженные файлы)

## Если нужно отменить изменения:

```bash
# Отменить добавление файла (но сохранить изменения)
git restore --staged <файл>

# Отменить все изменения в файле
git restore <файл>

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1
```

## Работа с удаленным репозиторием:

### Проверка удаленного репозитория:
```bash
git remote -v
```

### Если нужно добавить удаленный репозиторий:
```bash
git remote add origin <URL_репозитория>
```

### Если нужно изменить URL:
```bash
git remote set-url origin <новый_URL>
```

## Полезные команды:

```bash
# Просмотр истории коммитов
git log --oneline

# Просмотр изменений
git diff

# Просмотр изменений конкретного файла
git diff app.py

# Просмотр последнего коммита
git show HEAD
```


