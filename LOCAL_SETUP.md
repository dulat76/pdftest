# Настройка для локальной разработки (без PostgreSQL)

## ✅ Текущий статус

Супер-пользователь создан в локальной SQLite БД:
- **Логин:** `baseke`
- **Пароль:** `changeme123`

## 🚀 Запуск приложения

Просто запустите:
```bash
python app.py
```

Приложение автоматически использует локальную SQLite БД (`local_dev.db`) если PostgreSQL недоступен.

## 📝 Что было настроено

1. ✅ `models.py` - настроен на использование SQLite по умолчанию
2. ✅ Создана локальная БД `local_dev.db`
3. ✅ Создан супер-пользователь `baseke` с паролем `changeme123`
4. ✅ Проверена аутентификация - работает!

## 🔐 Вход в систему

1. Откройте http://localhost:5000
2. Нажмите "Я — Учитель"
3. Введите:
   - Логин: `baseke`
   - Пароль: `changeme123`

## 📌 Важные файлы

- `local_dev.db` - локальная SQLite база данных
- `.env` - файл с настройками (опционально, можно создать)

## 🔄 Если нужно использовать PostgreSQL

1. Убедитесь, что PostgreSQL доступен
2. Создайте файл `.env`:
   ```env
   DATABASE_URL=postgresql://user:password@host:port/database
   ```
3. Примените миграции: `alembic upgrade head`
4. Создайте супер-пользователя: `python init_superuser.py`



